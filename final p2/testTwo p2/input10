cd /tmp this and the next 6 should all cause complaints to be sent to stderr
echo 'unmatched quotes are an error, and should NOT cause a child to be forked
sort < /etc/shells < /etc/redhat-release
< somefile
/usr/bin/uname <
echo SHOULD_not_EXEC |
sort > &
echo This echo and the next cd SHOULD be executed by your shell and NOT cause errors
cd .. &
/usr/bin/pwd
